{
  "name": "cware-hil-obsidian",
  "version": "0.1.0",
  "description": "Obsidian bridge + UI for the cware human-in-the-loop hub",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node esbuild.config.mjs production",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node esbuild.config.mjs"
  },
  "dependencies": {
    "cware-hil-lib": "github:KilianSen/cware-hil-lib"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.2",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2"
  }
}
